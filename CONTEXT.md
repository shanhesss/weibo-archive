# CONTEXT

`weibo/` 微博拉取 + 查询工具的领域术语表。随决策敲定逐步补充。

## 术语

- **博主（账号）**：被拉取的任意公开微博账号，由 uid 唯一标识。工具同时管理多个博主。
- **博文**：一条已发布的微博。基础字段：正文、发布时间、媒体（图片/视频）URL、互动计数。
- **互动计数**：博文自带的转发数、评论数、点赞数，是拉取时刻的快照。
- **全量拉取**：首次拉取，从最新一页翻到翻不动为止（几千~几万条）。
- **增量拉取**：从最新一页向后翻，直到遇到库里已有的博文为止；由前端手动触发。
- **小号**：专门为拉取接口提供登录态的可牺牲微博账号；其 cookie（`SUB` 等）由浏览器登录后粘贴进工具。
- **通道**：拉取数据的接口路线。当前唯一通道：m.weibo.cn `getIndex`（containerid=230413 + uid，page+count 翻页）。
- **432**：微博反爬拦截的 HTTP 状态码，是「cookie 失效」或「请求过快」的信号；出现即暂停/提示重贴 cookie。
- **断点续爬**：全量拉取的翻页进度（next_page）随博主记录落库，工具重启后从该页继续；靠博文 ID 去重容忍页码漂移。
- **转发快照**：转发博文内嵌的被转原文精简快照（原文 id、作者昵称、正文、时间）；原文被删后快照仍在。
- **长文补拉**：列表接口检测到长文标记时，额外请求 `m.weibo.cn/detail/{id}` 取全文，避免静默截断。只对**新入库**的博文补拉；已存在的博文增量刷新仅更新互动数，不再重拉正文（避免每次增量都为整页长微博白等）。
- **图片代理**：server 端带 Referer 转发 sinaimg.cn 图片流，前端走本地代理 URL 看图（直连会 403 防盗链）；视频不做代理，留原文链接。
- **raw 留底**：每条博文除清洗后的结构化字段外，原始接口 JSON 一并落库，将来补字段无需重爬。见 ADR-0003。
- **卡片**：查询页的展示单元，3 列 × 2 行网格铺满视口；卡片内正文区可独立滚动。
- **缩略档**：卡片内图片使用微博 `bmiddle` 缩略图档位 URL，灯箱看大图仍用 `large` 原图。
- **灯箱**：点击缩略图后在当前页打开的大图浮层，点图切换本条微博图集内下一张、ESC/点遮罩关闭。
- **跳页**：分页条中输入页码直接翻页。
- **勾选集**：批量操作针对的对象；卡片勾选框 + 「全选本页」「全选当前筛选结果」，只对勾选条目生效。
- **覆盖更新**：对勾选博文整条重拉（正文/长文全文/计数/图片），新数据覆盖旧数据；是长文截断的存量修复手段。
- **并发拉取**：最多 2 个博主同时拉取（各自独立断点/进度/可暂停），另有 1 个专职通道专跑批量更新。
- **最早微博时间**：某博主已拉数据中时间最早一条的发布时间（到日），实时计算，展示在博主行昵称下方。
- **重拉全量**：不删数据、从头全量重拉覆盖；拉取时可选「起始日期」，拉取范围＝所选日期→今天，所选日期之前已入库的博文保持不动。拉取范围内微博上已不存在的博文标记为【博主已删除】。起始日期随任务存于博主记录的 pull_from 字段，暂停后「继续」会接着这个范围续拉。
- **添加博主（选起始日期）**：添加时点【添加并拉取】弹同款「起始日期」选择窗，默认从最早（微博上线日兜底）拉；所选起始日期同样存入 pull_from，拉取范围＝所选日期→今天。
- **博主排序**：博主行昵称右侧 ▲▼ 按钮上下移动列表顺序，顺序存于 bloggers 表 sort_order 字段（1 起连续编号），新增博主排在最后。
- **已删除标记**：博文的 deleted 状态；被任何拉取/更新见过的博文为正常，仅在全量拉到底后仍未被见到的被标记。可在筛选栏按「正常/已删除」过滤。
- **主页链接**：博主的标准主页 `https://weibo.com/u/{uid}`，添加时记录，可一键复制。
- **一键全部拉取**：「全部微博」行右侧按钮，为所有博主一次性拉取新微博；逐个按与单博主「拉取新的/继续」相同的规则入队（有未完成全量的续拉，否则增量），已在拉取/排队的自动跳过。前端记录本次实际启动的博主清单，显示「全部拉取中 x/y」进度，全部完成后一次提示，不再逐条打扰。

## 语雀归档

- **语雀归档**：把单条微博经 AI 总结后写入语雀知识库的单项操作，由卡片【同步】按钮手动触发。与「增量拉取」区分——增量拉取界面文案也叫「同步」，但指把微博拉到本地；本处归档指推到语雀。
- **归档目录**：博主行配置的语雀目录链接（`https://www.yuque.com/{账号}/{知识库}[/{层级}]/{目录}`），归档文档建在知识库并挂到该目录节点下（节点是目录还是文档都行，语雀允许文档挂文档）；可留空，留空时点击同步会提示先配置。
- **归档状态**：单条微博的归档标记与失败原因——待归档 / 已归档 / 同步失败 / 更新失败 / 无需归档；同步中 / 更新中为瞬态并落库（刷新不丢）。含语雀文档链接、归档时间、失败原因；驱动卡片按钮态、失败原因悬浮提示与「归档状态」筛选。失败可重试，也可批量改「无需归档 / 待归档」。
- **同步模板**：`weibo/yuque-sync-template.md`，定义语雀文档的标题与正文格式；每次同步前读取并按它生成内容，可直接编辑调整格式。
- **批量归档**：不限条数、批内 2 路并发，单卡与批量互不拦截（已在同步中的自动跳过、其余进等待队列、顶部计数累加），可随时取消；显示「同步中 x/y」；转发微博不支持归档，且拉取到转发微博时直接标为「无需归档」（不再显示同步按钮）；已归档微博再次同步 = 按最新模板更新已有语雀文档，不新建不重复。
